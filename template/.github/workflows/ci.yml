name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build library and validate TypeScript definitions
        run: |
          yarn
          yarn prepack
          yarn test
